# KISU Narrative Docs

These pages complement the generated Dokka API reference with task-oriented explanations.

- [Getting started](getting-started.md): construct measures, convert units, perform same-quantity arithmetic, and use common recipes.
- [Architecture overview](architecture.md): understand `Measure`, `Expression`, `Scalar`, `Product`, and `Quotient`.

For symbol-by-symbol details, generate the API reference from the repository root:

```bash
./gradlew :lib:dokkaGenerate
```
